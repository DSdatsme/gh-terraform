{
  "text": "GitHub Action ${{ github.workflow }} ${{ github.run_number }}",
  "attachments": [
    {
      "pretext": "Deployment waiting for approval...",
      "color": "dbab09",
      "fields": [
        {
          "title": "Status",
          "short": true,
          "value": "${{ job.status }}"
        }
      ]
    }
  ]
}
